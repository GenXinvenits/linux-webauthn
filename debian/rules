#!/usr/bin/make -f

export DEB_BUILD_MAINT_OPTIONS = hardening=+all

%:
	dh $@ --buildsystem=meson

# Meson already installs the production user unit and its vendor wants-link.
# Keep the user-service integration in /usr/lib/systemd/user and let
# dh_installsystemduser generate the package lifecycle maintainer snippets.
override_dh_installsystemduser:
	dh_installsystemduser linux-webauthn.service

# The upstream Meson install already places the exact 70-linux-webauthn.rules
# file. Re-run the udev helper with the same name so Debian generates the
# appropriate udev integration snippets without changing the installed name.
override_dh_installudev:
	dh_installudev --name=70-linux-webauthn
